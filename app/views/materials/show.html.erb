
  ID: <%= @material.id %> </br>
  Name: <%= @material.name %></br>
  Total Quantity: <%= @material.total_quantity %></br>
  Color: <%= @material.color %></br>
  Size: <%= @material.size %> </br>
  Current Batch: <%= @material.current_batch %></br>

  Add a batch for this material: 
  <%= link_to 'Add a Batch', new_material_batch_path(@material, @batch) %>  

<br>
<br>
These are the batches for this materal color/size:

<% @material.batches.each do |batch| %>
  <% if batch.current_quantity == nil || batch.current_quantity == 0 %>
    [Out of stock or depleted]
    ID: <%= batch.material_id %> <br>
    name: <%= batch.name %> <br>
    color: <%= batch.color %> <br>
    size: <%= batch.size %> <br>
    Original Quantity: <%= batch.original_quantity %> <br>
    Current Quantity: <%= batch.current_quantity %> <br>
    Total Cost: <%= batch.total_cost %> <br>
    Purchase Date: <%= batch.purchase_date %> <br>
    Cost per piece: <%= batch.cost_per_piece %> <br>
  <% else %>
    ID: <%= batch.material_id %> <br>
    name: <%= batch.name %> <br>
    color: <%= batch.color %> <br>
    size: <%= batch.size %> <br>
    Original Quantity: <%= batch.original_quantity %> <br>
    Current Quantity: <%= batch.current_quantity %> <br>
    Total Cost: <%= batch.total_cost %> <br>
    Purchase Date: <%= batch.purchase_date %> <br>
    Cost per piece: <%= batch.cost_per_piece %> <br>
  <% end %>
<% end %>